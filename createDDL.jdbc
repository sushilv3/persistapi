CREATE TABLE STUDENT (ID VARCHAR(32) NOT NULL, ADDRESS VARCHAR, DOB DATE, EMAIL VARCHAR, IS_ACTIVE BOOLEAN, MOBILE_NO VARCHAR, MODIFIED_BY VARCHAR, MODIFIED_ON DATE, NAME VARCHAR, PARENTS_EMAIL VARCHAR, PARENTS_MOBILE_NO VARCHAR, SEMESTER INTEGER, VERSION INTEGER, PRIMARY KEY (ID))
